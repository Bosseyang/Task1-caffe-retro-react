import type { ReactElement } from "react";

interface ITable {
    item: string;
    itemCount: number;
}

export default function TabelComp(props: ITable):ReactElement = {
    return ( array.forEach(element => {
        
    }););
};